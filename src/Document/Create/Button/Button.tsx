import React from 'react';
import classes from "./Button.module.css"

const Button = () => {
    return (
        <div className={classes.documentCreateButton}>
            Document Button
        </div>
    );
};

export {Button};
