import {Form} from "./Form"
import {Button} from "./Button"
import {Modal} from "./Modal"
export const DocumentCreate = {
    // Form, // internal - we wont export it
    Button,  // exported - lets assume it may come handy
    Modal
}
